# Admin portal is now bundled inside the Flutter web deploy workflow.
# It is served at: https://atchayamg.github.io/jyotish-ai/admin/
# This file is kept as a placeholder — do NOT re-enable it independently
# because GitHub Pages supports only ONE active deployment per repo.
#
# To update the admin portal: edit backend/admin/index.html and push to master.
# The flutter_web_deploy.yml workflow will rebuild everything together.

name: Admin Portal (disabled — bundled into flutter_web_deploy)

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is disabled. Admin is bundled in flutter_web_deploy.yml'
        required: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "Admin portal is bundled inside flutter_web_deploy.yml"
          echo "URL: https://atchayamg.github.io/jyotish-ai/admin/"
          echo "To redeploy, trigger flutter_web_deploy workflow instead."
